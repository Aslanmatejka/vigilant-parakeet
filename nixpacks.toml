# Nixpacks config — tells Railway to build as Python
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "python -m backend.run_forever"
